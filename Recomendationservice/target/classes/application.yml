server:
  port: 8050

spring:
  application:
    name: recommendation-service
  h2:
    console:
      enabled: true
      settings:
        web-allow-others: true
      path: /console
eureka:
  client:
    serviceUrl:
      defaultZone: http://localhost:8761/eureka/