server:
  port: 8060

spring:
  application:
    name: movie-service
  h2:
    console:
      enabled: true
      settings:
        web-allow-others: true
      path: /console
eureka:
  client:
    serviceUrl:
      defaultZone: http://localhost:8761/eureka/